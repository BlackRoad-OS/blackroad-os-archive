[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "blackroad-os-archive"

[services.env]
PORT = "8080"
NODE_ENV = "production"
REPO_NAME = "blackroad-os-archive"
ORG_NAME = "BlackRoad-OS"
